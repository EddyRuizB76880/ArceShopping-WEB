import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ArceShopping-WEB',
  webDir: 'dist/arce-shopping-web',
  bundledWebRuntime: false
};

export default config;
